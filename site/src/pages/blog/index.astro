---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog', ({ data }) => !data.draft);
const posts = allPosts.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
const [featured, ...rest] = posts;
---

<Layout title="Blog — AGE-QC" description="Analyses, guides et données sur le risque climatique immobilier.">
  <section class="blog-page">
    <header class="blog-header">
      <h1>Blog</h1>
      <p>Analyses, guides réglementaires et données sur le risque climatique immobilier.</p>
    </header>

    {featured && (
      <a href={`/blog/${featured.slug}/`} class="featured-card">
        {featured.data.image && <img src={featured.data.image} alt={featured.data.title} />}
        <div class="featured-content">
          {featured.data.tags.length > 0 && (
            <div class="tags">
              {featured.data.tags.map((tag) => <span class="tag">{tag}</span>)}
            </div>
          )}
          <h2>{featured.data.title}</h2>
          <p>{featured.data.description}</p>
          <time datetime={featured.data.pubDate.toISOString()}>
            {featured.data.pubDate.toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' })}
          </time>
        </div>
      </a>
    )}

    <div class="posts-grid">
      {rest.map((post) => (
        <a href={`/blog/${post.slug}/`} class="post-card">
          {post.data.image && <img src={post.data.image} alt={post.data.title} />}
          {post.data.tags.length > 0 && (
            <div class="tags">
              {post.data.tags.map((tag) => <span class="tag">{tag}</span>)}
            </div>
          )}
          <h3>{post.data.title}</h3>
          <p>{post.data.description}</p>
          <time datetime={post.data.pubDate.toISOString()}>
            {post.data.pubDate.toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' })}
          </time>
        </a>
      ))}
    </div>
  </section>
</Layout>

<style>
  .blog-page {
    max-width: 1200px;
    margin: 0 auto;
    padding: 48px 24px 80px;
  }

  .blog-header {
    margin-bottom: 40px;
  }

  .blog-header h1 {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 40px;
    font-weight: 600;
    color: #111111;
    margin: 0 0 10px;
  }

  .blog-header p {
    font-family: 'Inter', sans-serif;
    font-size: 17px;
    color: #555;
    max-width: 560px;
  }

  .featured-card {
    display: grid;
    grid-template-columns: 1.1fr 1fr;
    gap: 0;
    text-decoration: none;
    color: inherit;
    margin-bottom: 40px;
    border-radius: 16px;
    overflow: hidden;
    background: #fff;
    border-top: 4px solid #3F7A5D;
    box-shadow: 0 4px 20px rgba(17, 17, 17, 0.09);
    transition: box-shadow 0.2s ease, transform 0.2s ease;
  }

  .featured-card:hover {
    box-shadow: 0 8px 28px rgba(17, 17, 17, 0.14);
    transform: translateY(-2px);
  }

  .featured-card img {
    width: 100%;
    height: 100%;
    min-height: 300px;
    object-fit: cover;
  }

  .featured-content {
    padding: 36px 40px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .featured-content h2 {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 26px;
    font-weight: 600;
    margin: 14px 0;
    color: #111111;
    line-height: 1.3;
  }

  .featured-content p {
    font-family: 'Inter', sans-serif;
    font-size: 15px;
    color: #444;
    line-height: 1.55;
    margin: 0 0 18px;
  }

  .posts-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }

  .post-card {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    color: inherit;
    border-radius: 12px;
    overflow: hidden;
    background: #fff;
    box-shadow: 0 2px 12px rgba(17, 17, 17, 0.07);
    transition: box-shadow 0.2s ease, transform 0.2s ease;
  }

  .post-card:hover {
    box-shadow: 0 6px 20px rgba(17, 17, 17, 0.12);
    transform: translateY(-2px);
  }

  .post-card img {
    width: 100%;
    height: 180px;
    object-fit: cover;
  }

  .post-card h3 {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 19px;
    font-weight: 600;
    margin: 18px 20px 8px;
    color: #111111;
    line-height: 1.3;
  }

  .post-card p {
    font-family: 'Inter', sans-serif;
    font-size: 14px;
    color: #555;
    line-height: 1.5;
    margin: 0 20px 14px;
  }

  .tags {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin: 18px 20px 0;
  }

  .featured-content .tags {
    margin: 0;
  }

  .tag {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    padding: 4px 11px;
    border-radius: 20px;
    background: #E1F5EE;
    color: #0F6E56;
  }

  time {
    font-family: 'JetBrains Mono', monospace;
    font-size: 12px;
    color: #888;
    margin: 0 20px 20px;
    display: block;
  }

  .featured-content time {
    margin: 4px 0 0;
  }

  @media (max-width: 768px) {
    .featured-card {
      grid-template-columns: 1fr;
    }
    .posts-grid {
      grid-template-columns: 1fr;
    }
  }
</style>